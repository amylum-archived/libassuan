Prospectus.extra_dep('file', 'prospectus_pkgforge')

item do
  expected do
    url_xpath
    url 'http://git.gnupg.org/cgi-bin/gitweb.cgi?p=libassuan.git;a=tags'
    xpath '/html/body/table/tr[1]/td[2]/a'
    regex /^libassuan-([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  extend ProspectusPkgforge::DepItems
end

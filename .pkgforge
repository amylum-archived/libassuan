name 'libassuan'
org 'amylum'

licenses 'COPYING.LIB'

deps(
  'libgpg-error': '1.26-1'
)

configure_flags(
  prefix: '/usr',
  'enable-maintainer-mode': nil
)
remove_linker_archives
remove_pkgconfig_files

cflags
harden

build do
  run './autogen.sh'
  configure
  make
  install
end

test do
  # TODO: add tests
end
